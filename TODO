To-do list
1. change the method to end the thread to a recommended method rather than the poor 'terminate' statement
2. look into maintaining bandwidth between gqrx and hamlib (waiting for this feature to be release in gqrx)
